$('turbolinks:load').ready(function() {
  <!-- Recarrega o datatable usando a chamada AJAX -->
  App.Todo.refreshDataTableAjax();

  <!-- Exibe mensagem de sucesso -->
  Toast.success("Remoção em lote realizada com sucesso!")
});
